import { Box, Container, Heading } from "@chakra-ui/react";

const FavoriteFieldsPage = () => {

    return (
        <Container centerContent={true}>
            <Box >
                <Heading>Favorite Fields Page</Heading>
            </Box>
        </Container>
    );
};

export default FavoriteFieldsPage;